<Window x:Class="Finly.Shell.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:conv="clr-namespace:Finly.Helpers.Converters"
        Title="Finly" WindowStyle="None" WindowState="Maximized"
        Background="{DynamicResource App.Background}">

    <Window.Resources>

        <!-- Fallback kolory, gdyby nie było ich w Shared/Dark -->
        <SolidColorBrush x:Key="FallbackSidebarFg" Color="#EDEDED"/>
        <SolidColorBrush x:Key="FallbackHover"     Color="#22FFFFFF"/>
        <SolidColorBrush x:Key="FallbackOrange"    Color="#FFED7A1A"/>

        <!-- (zostaw, jeśli już masz) mały styl przycisków paska tytułu -->
        <Style x:Key="TitleBarIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <!-- ✅ Fallback NAV BUTTON (pomarańczowe zakładki w sidebarze) -->
        <Style x:Key="NavButton" TargetType="ToggleButton">
            <!-- próbuj użyć kolorów z motywu; jak brak – weź fallback -->
            <Setter Property="Foreground" Value="{DynamicResource Sidebar.Foreground}"/>
            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self},
                                         Path=Foreground,
                                         FallbackValue={StaticResource FallbackSidebarFg}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter Margin="10,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource Nav.Hover}"/>
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource FallbackHover}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource Brand.Orange}"/>
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource FallbackOrange}"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- (opcjonalnie) przyciski na dole sidebara -->
        <Style x:Key="SidebarActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource Sidebar.Foreground}"/>
            <Setter Property="Foreground" Value="{StaticResource FallbackSidebarFg}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter Margin="6,2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource Nav.Hover}"/>
                                <Setter TargetName="bg" Property="Background" Value="{StaticResource FallbackHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>


    <!-- Umożliwia przeciąganie/zmianę rozmiaru przy własnym tytule -->
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="48"
                            ResizeBorderThickness="6"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <!-- Pasek tytułu -->
            <RowDefinition Height="*"/>
            <!-- Zawartość -->
        </Grid.RowDefinitions>

        <!-- ===== PASEK TYTUŁU ===== -->
        <Grid Grid.Row="0" Background="{DynamicResource TitleBar.Background}"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <!-- tytuł/lewa strona -->
                <ColumnDefinition Width="Auto"/>
                <!-- przyciski okna -->
            </Grid.ColumnDefinitions>

            <TextBlock Text="Finly" VerticalAlignment="Center" Margin="12,0,0,0"
                       Foreground="{DynamicResource App.Foreground}" FontWeight="SemiBold"/>

            <!-- Systemowe przyciski okna -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Width="46" Height="32" Padding="0"
                        Background="Transparent" BorderBrush="{x:Null}"
                        Click="Minimize_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <TextBlock Text="—" FontSize="16" VerticalAlignment="Center"
                               HorizontalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                </Button>
                <Button Width="46" Height="32" Padding="0"
                        Background="Transparent" BorderBrush="{x:Null}"
                        Click="MaxRestore_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <TextBlock Text="⬜" FontSize="12" VerticalAlignment="Center"
                               HorizontalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                </Button>
                <Button Width="46" Height="32" Padding="0"
                        Background="Transparent" BorderBrush="{x:Null}"
                        Click="Close_Click" shell:WindowChrome.IsHitTestVisibleInChrome="True">
                    <TextBlock Text="✕" FontSize="12" VerticalAlignment="Center"
                               HorizontalAlignment="Center" Foreground="{DynamicResource App.Foreground}"/>
                </Button>
            </StackPanel>
        </Grid>
        <!-- ===== /PASEK TYTUŁU ===== -->

        <!-- ===== TREŚĆ: SIDEBAR + CONTENT ===== -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <!-- Sidebar -->
                <ColumnDefinition Width="*"/>
                <!-- Content -->
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR -->
            <Border Grid.Column="0" Background="{DynamicResource Sidebar.Background}">
                <Grid Margin="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- logo -->
                        <RowDefinition Height="*"/>
                        <!-- nawigacja -->
                        <RowDefinition Height="Auto"/>
                        <!-- akcje konta -->
                    </Grid.RowDefinitions>

                    <!-- GÓRA: klikane LOGO prowadzi na stronę główną -->
                    <StackPanel Grid.Row="0" Margin="16,16,16,8">
                        <Button Click="Logo_Click" Background="Transparent" BorderBrush="{x:Null}" Cursor="Hand">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Ellipse Width="48" Height="48" Fill="{DynamicResource Brand.Orange}"/>
                                <TextBlock Text="  Finly" Margin="6,0,0,0" FontSize="18" FontWeight="Bold"
                                           Foreground="{DynamicResource Sidebar.Foreground}"/>
                            </StackPanel>
                        </Button>
                        <Separator Margin="0,12,0,8" Background="{DynamicResource Surface.Border}"/>
                    </StackPanel>

                    <!-- NAWIGACJA (zakładki pomarańczowe wg stylu NavButton) -->
                    <StackPanel Grid.Row="1" Margin="8,0,8,8" x:Name="NavContainer">
                        <ToggleButton x:Name="NavAdd"            Style="{StaticResource NavButton}" Content="Dodaj wydatek" Click="Nav_Add_Click"/>
                        <ToggleButton x:Name="NavTransactions"   Style="{StaticResource NavButton}" Content="Transakcje"   Click="Nav_Transactions_Click"/>
                        <ToggleButton x:Name="NavCharts"         Style="{StaticResource NavButton}" Content="Wykresy"      Click="Nav_Charts_Click"/>
                        <ToggleButton x:Name="NavBudgets"        Style="{StaticResource NavButton}" Content="Budżety"      Click="Nav_Budgets_Click"/>
                        <ToggleButton x:Name="NavSubscriptions"  Style="{StaticResource NavButton}" Content="Subskrypcje"  Click="Nav_Subscriptions_Click"/>
                        <ToggleButton x:Name="NavGoals"          Style="{StaticResource NavButton}" Content="Cele"         Click="Nav_Goals_Click"/>
                        <ToggleButton x:Name="NavCategories"     Style="{StaticResource NavButton}" Content="Kategorie"    Click="Nav_Categories_Click"/>
                        <ToggleButton x:Name="NavReports"        Style="{StaticResource NavButton}" Content="Raporty"      Click="Nav_Reports_Click"/>
                        <ToggleButton x:Name="NavImport"         Style="{StaticResource NavButton}" Content="Import / Sync" Click="Nav_Import_Click"/>
                        <!-- Uwaga: Ustawienia przeniesione na dół, więc brak NavSettings -->
                    </StackPanel>

                    <!-- DÓŁ: Konto / Ustawienia / Wyloguj -->
                    <StackPanel Grid.Row="2" Margin="8,8,8,16">
                        <Separator Margin="0,8,0,12" Background="{DynamicResource Surface.Border}"/>

                        <!-- Konto (avatar) -->
                        <Button Style="{StaticResource SidebarActionButton}" Click="OpenProfile_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE77B;" Margin="0,0,8,0"/>
                                <TextBlock Text="Konto"/>
                            </StackPanel>
                        </Button>

                        <!-- Ustawienia -->
                        <Button Style="{StaticResource SidebarActionButton}" Margin="0,6,0,0" Click="OpenSettings_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" Margin="0,0,8,0"/>
                                <TextBlock Text="Ustawienia"/>
                            </StackPanel>
                        </Button>

                        <!-- Wyloguj -->
                        <Button Style="{StaticResource SidebarActionButton}" Margin="0,6,0,0" Click="Nav_Logout_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xF3B1;" Margin="0,0,8,0"/>
                                <TextBlock Text="Wyloguj"/>
                            </StackPanel>
                        </Button>

                        <TextBlock Text="v1.0" Opacity="0.6" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- CONTENT -->
            <Grid Grid.Column="1" Background="{DynamicResource App.Background}">
                <ContentControl x:Name="RightHost"/>
            </Grid>
        </Grid>
        <!-- ===== /TREŚĆ ===== -->
    </Grid>
</Window>
