<Window x:Class="Finly.Views.ChartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="http://livecharts.com"
        Title="Wykres wydatków" Width="1200" Height="760"
        WindowStartupLocation="CenterOwner">
    <Grid Background="{DynamicResource App.Background}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- FILTRY -->
        <Border Style="{StaticResource SurfaceCard}" Padding="14">
            <UniformGrid Rows="1" Columns="7" Margin="0" HorizontalAlignment="Stretch">
                <StackPanel>
                    <TextBlock Text="Sortuj" Style="{StaticResource Caption}"/>
                    <ComboBox x:Name="SortComboBox" SelectionChanged="SortComboBox_SelectionChanged">
                        <ComboBoxItem Content="Kwota rosnąco"/>
                        <ComboBoxItem Content="Kwota malejąco"/>
                        <ComboBoxItem Content="Data rosnąco"/>
                        <ComboBoxItem Content="Data malejąco"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Sortowanie wg daty" Style="{StaticResource Caption}"/>
                    <TextBlock Text=" "/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Od" Style="{StaticResource Caption}"/>
                    <DatePicker x:Name="FromDatePicker"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Do" Style="{StaticResource Caption}"/>
                    <DatePicker x:Name="ToDatePicker"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Kategoria" Style="{StaticResource Caption}"/>
                    <ComboBox />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Minimalna kwota" Style="{StaticResource Caption}"/>
                    <TextBox />
                </StackPanel>

                <StackPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="Filtruj" Click="FilterButton_Click" />
                </StackPanel>
            </UniformGrid>
        </Border>

        <!-- WYKRES: kategorie -->
        <Border Grid.Row="1" Style="{StaticResource SurfaceCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Wydatki wg kategorii" Style="{StaticResource CardHeader}"/>
                <lvc:PieChart x:Name="pieChart" Grid.Row="1"
                      LegendPosition="Right"
                      DataTooltip="{x:Null}" />
            </Grid>
        </Border>

        <!-- WYKRES: w czasie -->
        <Border Grid.Row="2" Style="{StaticResource SurfaceCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Wydatki wg daty" Style="{StaticResource CardHeader}"/>

                <lvc:CartesianChart x:Name="lineChart" Grid.Row="1" DrawMarginFrame="{x:Null}">
                    <lvc:CartesianChart.XAxes>
                        <lvc:Axis LabelsRotation="0" TextSize="14" />
                    </lvc:CartesianChart.XAxes>
                    <lvc:CartesianChart.YAxes>
                        <lvc:Axis TextSize="14" />
                    </lvc:CartesianChart.YAxes>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</Window>
