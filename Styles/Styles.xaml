<!-- Styles/Styles.xaml -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<!-- Teksty -->
	<Style TargetType="TextBlock">
		<Setter Property="Foreground" Value="{DynamicResource App.Foreground}"/>
	</Style>

	<!-- Pasek nawigacji -->
	<Style x:Key="NavBar" TargetType="Border">
		<Setter Property="Background" Value="{DynamicResource Nav.Background}"/>
		<Setter Property="Height"     Value="60"/>
	</Style>
    <!-- ✅ CheckBox: biały ptaszek, bez tła -->
    <SolidColorBrush x:Key="Accent" Color="#4F7AF7"/>
    <SolidColorBrush x:Key="OnAccent" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="Surface.Background" Color="#1E1E1E"/>
    <SolidColorBrush x:Key="Surface.Border" Color="#3A3A3A"/>
    <SolidColorBrush x:Key="Nav.Foreground" Color="#F3F3F3"/>

    <Style x:Key="CheckBox.Accent" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Nav.Foreground}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" SnapsToDevicePixels="True">
                        <!-- Kwadracik -->
                        <Grid Width="18" Height="18" x:Name="GlyphGrid">
                            <Border x:Name="Box"
                                CornerRadius="3"
                                Background="{DynamicResource Surface.Background}"
                                BorderBrush="{DynamicResource Surface.Border}"
                                BorderThickness="1"/>
                            <!-- Ptaszek (biały) -->
                            <Path x:Name="CheckMark"
                              Data="M 3 9 L 7 13 L 15 5"
                              StrokeThickness="2.2"
                              StrokeLineJoin="Round"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              Stroke="{DynamicResource OnAccent}"
                              Opacity="0"/>
                        </Grid>

                        <ContentPresenter Margin="8,0,0,0"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                    </StackPanel>

                    <ControlTemplate.Triggers>
                        <!-- ✅ Zaznaczenie: tylko ptaszek; tło bez zmian -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                            <!-- opcjonalnie delikatne podbicie obramowania -->
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                            <Setter TargetName="Box" Property="BorderThickness" Value="1.5"/>
                        </Trigger>

                        <!-- Hover: subtelny akcent na obrysie -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Box" Property="Opacity" Value="0.55"/>
                            <Setter Property="Foreground" Value="#7F9AA3"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>
